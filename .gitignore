# Obsidian configuration
.obsidian/

# macOS specific
.DS_Store
.vscode/
.history/

# Mage's Personal Workspace
# Ignore the contents of these directories...
scrolls/*
floor/*
box/*
desk/*
# ...but not the directories themselves (via .gitkeep).
!scrolls/.gitkeep
!floor/.gitkeep
!box/.gitkeep
!desk/.gitkeep

# Portal Infrastructure (Shared Practice Spaces)
# Portals are external repositories in portals/
# Each portal directory is ignored (separate git repo)
# but the template and infrastructure are tracked
portals/*/
portals/portals.yaml
!portals/portals.yaml.template
!portals/README.md

# Personal, untracked ritual artifacts
# floor/essences/

# Mage's personal configuration
mage_seal.md
archive/*
!archive/.gitkeep
system/tomes/librarian/
